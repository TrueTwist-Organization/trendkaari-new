/** In-feed ad rhythm — category grid vs horizontal suggestion rails */

/** Main category product grid — ad after every Nth card */
export const CATEGORY_GRID_AD_EVERY_N = 2;

/** Horizontal suggestion rails (category hub + product page below-fold) */
export const SUGGESTION_RAIL_AD_EVERY_N = 6;

/** @deprecated Use SUGGESTION_RAIL_AD_EVERY_N */
export const PRODUCT_RAIL_AD_EVERY_N = SUGGESTION_RAIL_AD_EVERY_N;
